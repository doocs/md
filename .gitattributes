# These declaration files are regenerated by unplugin-auto-import /
# unplugin-vue-components with LF on every dev-server start. Pin LF so
# core.autocrlf on Windows doesn't flip them to CRLF and mark them dirty.
apps/web/auto-imports.d.ts text eol=lf
apps/web/components.d.ts text eol=lf
